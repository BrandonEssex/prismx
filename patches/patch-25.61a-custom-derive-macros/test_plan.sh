#!/bin/bash

Patch 25.61a Test Plan

Build project – ensure prismx_macros compiles with no warnings
Annotate a sample UI struct with #[derive(Widget)]
Confirm trait Renderable is implemented automatically
Run app – ensure Zen or test module still renders with no crash
