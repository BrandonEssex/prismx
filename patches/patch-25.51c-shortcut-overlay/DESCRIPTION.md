# Patch 25.51c – Shortcut Overlay + Cheatsheet Sync

## Goals
- Update `SHORTCUTS` array in `src/ui/shortcuts.rs`
- Regenerate `render_shortcuts_overlay.rs` if needed
- Sync `CHEATSHEET.md` with overlay
