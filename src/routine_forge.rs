use crate::state::AppState;

pub fn schedule_morning_routine(state: &mut AppState) {
    // Placeholder for custom routine logic
    println!("Morning routine scheduled.");
    state.sidebar_visible = true;
}